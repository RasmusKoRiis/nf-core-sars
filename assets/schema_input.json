{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/sars/master/assets/schema_input.json",
    "title": "nf-core/sars pipeline - params.input schema",
    "description": "Schema for the FASTQ workflow samplesheet provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "SequenceID": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "SequenceID must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "Barcode": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Barcode must be provided and cannot contain spaces"
            },
            "KonsCt": {
                "type": ["string", "number", "null"],
                "description": "Optional Ct value carried through the samplesheet metadata."
            }
        },
        "required": ["SequenceID", "Barcode"]
    }
}
